# FL-09-Nov-2015
# pip requirements file for hsnmailenbeheer with a Python-2.7.10
# setuptools, pip, and virtualenv should already be present in the (virtual) python2.7
# python-devel and mysql-devel were needed for installing mysqlclient
# install the packages below from a terminal: 
# $ pip install -r requirements_py2.7.txt

future >= 0.15.2
Django >= 1.8.6
mysqlclient >= 1.3.7
pycups >= 1.9.73
https://github.com/ubernostrum/django-registration/archive/master.zip

# [eof]
